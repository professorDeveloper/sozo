<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Casting. Discovery is mDNS over multicast, which the radio drops by
         default to save power, and the receiver fetches the media from this
         phone over the local network. Android 12+ additionally gates seeing
         nearby devices behind its own permission; neverForLocation says what is
         true, that this is about a television and not about where anyone is. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Voice search. The <queries> entry below is what makes it work on
         API 30+: package visibility hides the speech recogniser otherwise, and
         the failure is silent — the plugin simply reports no recogniser. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <!-- Android TV. Every feature below is required="false" ON PURPOSE: this is
         one APK that installs and behaves identically on phones/tablets and
         ADDITIONALLY appears on the Leanback launcher. required="true" anywhere
         here would turn this into a TV-only APK and strip it from phones. -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <!-- Touchscreen is implicitly required="true" for every app that stays
         silent about it. Without this line no TV can install or run Sozo. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <!-- android.permission.USE_FINGERPRINT (above) carries an implicit REQUIRED
         android.hardware.fingerprint feature. No TV has a fingerprint reader,
         so neutralise it explicitly. (USE_BIOMETRIC implies nothing.) -->
    <uses-feature android:name="android.hardware.fingerprint" android:required="false"/>

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>

    <application
        android:label="Sozo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- Additive: LAUNCHER stays, so phone/tablet launchers are
                     unaffected. LEANBACK_LAUNCHER is what makes the same
                     activity appear on the Android TV home screen. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="sozo.azamov.me"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="sozo"/>
            </intent-filter>

            <!-- "Open with Sozo" for extension repository index files.

                 Extension repos are distributed as one index file: index.pb
                 (Mihon/Keiyoushi, gzipped protobuf), index.min.json (Aniyomi and
                 older forks), repo.json (CloudStream) or index.json (Mangayomi).
                 These filters put Sozo in the chooser when one is tapped in a
                 file manager, a browser download, or a chat app.

                 Why the mimeType is a wildcard: senders label these
                 inconsistently — Telegram sends application/octet-stream, Chrome
                 sends application/json or text/plain, some file managers send
                 nothing at all. Matching on the path pattern and re-checking the
                 filename in RepoFileIntent is the only combination that actually
                 catches them; a strict mimeType silently drops most real cases.

                 pathPattern's ".*" is NOT greedy-safe across "." in Android's
                 matcher, so each extension needs its own leading-wildcard entry. -->
            <intent-filter android:label="@string/open_repo_file">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:host="*"/>
                <data android:mimeType="*/*"/>
                <data android:pathPattern=".*\\.pb"/>
                <data android:pathPattern=".*\\..*\\.pb"/>
                <data android:pathPattern=".*index\\.json"/>
                <data android:pathPattern=".*index\\.min\\.json"/>
                <data android:pathPattern=".*repo\\.json"/>
                <data android:pathPattern=".*anime_index\\.json"/>
                <data android:pathPattern=".*novel_index\\.json"/>
            </intent-filter>
            <!-- Same, for a direct link to a raw index (e.g. a GitHub raw URL
                 pasted into a chat). Not autoVerify: these are third-party hosts
                 we cannot claim, so the user picks Sozo from the chooser. -->
            <intent-filter android:label="@string/open_repo_file">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.pb"/>
                <data android:pathPattern=".*index\\.min\\.json"/>
                <data android:pathPattern=".*anime_index\\.json"/>
                <data android:pathPattern=".*novel_index\\.json"/>
            </intent-filter>
            <!-- Shared into Sozo from another app ("Share → Sozo"). -->
            <intent-filter android:label="@string/open_repo_file">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="application/json"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <service
            android:name=".DownloadForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <!-- External playback handoff (Settings → Player → External player).
             Without this, Android 11+ package visibility hides every installed
             video app, resolveActivity() returns null and the chooser never
             appears — the feature would silently do nothing. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="video/*" />
        </intent>
        <!-- "Open in torrent app" on a search result. Same package-visibility
             rule as the video handoff above: without this the magnet intent
             resolves to nothing on Android 11+ even with LibreTorrent or Flud
             installed, and the menu item would quietly do nothing. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="magnet" />
        </intent>
    </queries>
</manifest>
